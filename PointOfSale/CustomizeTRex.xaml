<Page x:Class="PointOfSale.CustomizeTRex"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PointOfSale"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CustomizeTRex">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="1" Click="OnHoldLettuce">Hold Lettuce</Button>
        <Button Click="OnHoldBun">Hold Bun</Button>
        <Button Grid.Column="2" Click="OnHoldOnions">Hold Onions</Button>
        <Button Grid.Row="1" Click="OnHoldPickle">Hold Pickle</Button>
        <Button Grid.Column="1" Grid.Row="1" Click="OnHoldTomato">Hold Tomato</Button>
        <Button Grid.Row="2" Click="OnHoldKetchup">Hold Ketchup</Button>
        <Button Grid.Row="2" Grid.Column="1" Click="OnHoldMustard">Hold Mustard</Button>
        <Button Grid.Column="2" Grid.Row="2" Click="OnDone">Done</Button>
    </Grid>
</Page>
